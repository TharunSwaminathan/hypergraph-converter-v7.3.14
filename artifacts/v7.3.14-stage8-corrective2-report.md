# Stage 8 corrective #2 validation

- Authoritative parent: `ea6c442b5219f7452fd1dc32663cec580790911e`
- Pre-fix evidence commit: `0a1d663cf274ba50d7289d9285f6ade351f41857`
- Production corrective commit: `c5bea11f5de917785eebbae35ad3c10a54ae2367`
- Test migration and preservation commit: `2219fad45cdd1e6c75002cdc67fdad6af2a5cb62`
- Package version: `7.3.13`
- Stage 9 begun: false.

## Findings and correction

- S8-N03 (High) was reproduced before the production change. Graph, dashboard, mapping, parser-workflow, and legacy typed plans now derive their effective side-effect class from the actual typed value, operation types, canonical intent, and registered legacy metadata. A state-changing plan whose declared class is missing or inconsistent is rejected at `deterministic_final_side_effect_gate` with `side_effect_class_mismatch` before any protected handler.
- S8-N04 (Medium) was reproduced before the production change. The state-changing `legacy_prepared_action_without_runtime_semantics` exemption was removed. A state-changing prepared plan with no query/request semantics now fails closed with `missing_request_semantics` and reaches no state-changing handler. Read-only compatibility remains non-mutating.
- Final authorization is evaluated against the derived actual class. A caller-supplied `read_only`, `navigation`, unrelated valid class, `unknown`, or missing class cannot act as an authorization escape hatch for a different state-changing typed plan.
- Truncated authorization remains fail-closed for every actual state-changing class.

## Adversarial final-gate matrix

The permanent matrix crosses five actual typed-plan families with five declared-class variants and five authorization variants: 125 cases total. Exactly five cases were expected to execute and exactly those five did. The matrix recorded zero violations, 100 declared/actual mismatch blocks, five missing-semantics blocks, and 25 truncation blocks. Every protected handler count and protected-state fingerprint matched the oracle.

The model/fallback regression deliberately paired `Use CSR.` with a `GraphMutationPlan/CLEAR_GRAPH` while falsely declaring `navigation`. The final gate rejected the mismatch; no graph handler or pending confirmation was reached and protected state did not change.

## Approved Stage 8 preservation

- S8-N01 mixed corpus: 1,135 cases; zero unauthorized side effects, zero missed authorized clauses, zero wrong side-effect scopes, and zero state-fingerprint violations.
- S8-N02 truncation probes: all eight >5,000-character cases failed closed; zero handler calls and zero protected-state violations.
- Existing read-only corpus: 9,840/9,840 safe.
- Independent read-only corpus: 6,560/6,560 safe.
- Positive corpus: 492 cases with zero wrong blocks.
- Pending corpus: 30/30 read-only requests preserved the pending action; 3/3 genuine corrections succeeded.
- Model/fallback corpus: eight adversarial cases with zero malicious execution.

## Real Chrome validation

Eight focused flows passed in real Google Chrome through the Codex computer-use extension at `http://127.0.0.1:5173/`: direct graph staging, real pending-plan replacement, confirm, cancel, read-only graph wording, mixed Explain CSR then Use CSR routing, a schema-valid model `CLEAR_GRAPH` plan under a read-only request, and an authorized model-planned Statistics navigation. Three Chrome tabs were inspected and reported zero application console errors.

The local-model cases used a temporary loopback-only Ollama-compatible fixture at `127.0.0.1:11434`. It returned a schema-valid adversarial graph plan and a schema-valid navigation plan, stored no data, contacted no external service, and was stopped and removed after validation.

## Command validation

- Full Node suite: passed, 212/212 files in 80.2 seconds. The existing v7.3.11 ZIP-permission test was skipped because `python3` was unavailable in this environment.
- Lint: passed.
- Production build: passed with the existing Vite >500 kB chunk-size warning only.
- GitHub build: passed with the same existing warning only.
- Full `npm audit`: one high-severity dev-only `browserslist <=4.28.6` advisory. No dependency was changed because this corrective explicitly prohibits dependency changes.
- Production dependency audit (`npm audit --omit=dev`): zero vulnerabilities.
- `git diff --check`: passed.

## Scope closure

The package and lockfile remain at version `7.3.13`, dependency manifests are unchanged from the authoritative parent, and the clause parser was not modified. Only the Stage 8 corrective #2 final-dispatch authorization boundary, narrowly required test migrations, preservation coverage, and evidence were changed. Stage 9 was not begun.
