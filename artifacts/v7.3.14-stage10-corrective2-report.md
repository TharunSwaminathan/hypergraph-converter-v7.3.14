# Hypergraph Converter Studio v7.3.14 — Stage 10 Corrective #2

## Result

S10-N01 and S10-N02 are `closed_pending_independent_review`. The final Stage 10 rerun was not begun.

The correction replaces broad `graph_edit_preview` authority with clause-local, operation-bound authorization. Contraction normalization and deterministic grammatical framing distinguish present affirmative execution directives from negated speech acts, negative complements, denied or withdrawn permission, epistemic/decision framing, reported intent, future intent, and past discussion. The final dispatcher reconstructs authority from the original user query and compares every resolved state-changing operation to deterministic descriptors covering operation family, identifiers, targets, and material parameters. Prepared/model-shaped plan claims do not create authority, and a multi-operation plan fails closed if any operation lacks a matching affirmative descriptor.

## Adversarial evidence

- Exact original S10-N01: 0 protected handler calls.
- Fresh held-out no-consent corpus: 389 cases; 0 wrong positive authorizations, protected handler calls, confirmation stages, or state changes; 9/9 true-positive controls accepted.
- Operation-binding matrix: 21 cases; all 12 invalid plans blocked before the protected handler; 9/9 exact matching plans executed through preview/confirmation.
- Invalid substitutions blocked: 2 operation-family, 5 target, 2 parameter, and 3 mixed authorized/denied cases.
- Stage 10 Corrective #1 corpus preserved: 640 no-consent plus 11 positive cases, with every violation counter zero.
- Stage 8 gates preserved at their approved counts, including S8-N03 through S8-N06.

## Regression and dependency qualification

- Node suite: 219/219 files passed in the source tree and a fresh extraction.
- Lint: passed.
- Normal and GitHub Pages builds: passed; Vite reported only the existing bundle chunk-size warning.
- Full audit: one high-severity, transitive development-only Browserslist advisory (`GHSA-c83g-rgw3-j3cx` and `GHSA-73wf-gq98-2v4g`); a fix is available, but dependency changes were not authorized.
- Production audit: 0 vulnerabilities.
- No package or lockfile changes; package version remains 7.3.14.

## Portable package

`hypergraph-converter-v7.3.14-stage10-corrective2-portable.zip` is 1,210,451 bytes with SHA-256 `d653efe384696775ca1f98c081c9cf5a0f2cf748bdc782087f4d18b4829c1a23`.

The archive has 660 members under the single `hypergraph-converter-v7.3.14` root. CRC/testzip passes. R08 remains closed: no absolute, traversal, backslash, duplicate, case-collision, symlink, special, forbidden, unexpected-executable, group/world-writable, or non-Unix metadata entries were found. Directories and launcher entries are 0755; ordinary files are 0644. Two Windows and two WSL/Ubuntu packings produced the identical SHA-256.

## Clean extraction and browser

A clean `npm ci` added 144 packages. The corrective tests ran as part of the clean 219/219 Node suite; lint, both builds, and both audits reproduced the source-tree results.

Real Chrome loaded a two-hyperedge graph, displayed Graph Preview, ran Connected Components, and generated a CSR CSV preview. The exact original and both requested held-out no-consent utterances staged no mutation confirmation. The valid create-hyperedge utterance staged preview/confirmation only and was cancelled, leaving graph version 1 and two hyperedges. Application-origin console errors: 0.

## Remaining limitations

The development dependency audit remains non-zero because of the transitive Browserslist advisory, and the production JavaScript bundle still triggers Vite's chunk-size advisory. Dependency changes and broader performance refactoring were outside this corrective's authorization.
