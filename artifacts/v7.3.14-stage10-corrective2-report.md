# Hypergraph Converter Studio v7.3.14 — Stage 10 Corrective #2

## Result

S10-N01 and S10-N02 are `closed_pending_independent_review`. The final Stage 10 rerun was not begun.

The correction replaces broad `graph_edit_preview` authority with clause-local, operation-bound authorization. Contraction normalization and deterministic grammatical framing distinguish present affirmative execution directives from negated speech acts, negative complements, denied or withdrawn permission, epistemic/decision framing, reported intent, future intent, and past discussion. The final dispatcher reconstructs authority from the original user query and compares every resolved state-changing operation to deterministic descriptors covering operation family, identifiers, targets, and material parameters. Prepared/model-shaped plan claims do not create authority, and a multi-operation plan fails closed if any operation lacks a matching affirmative descriptor.

## Adversarial evidence

- Exact original S10-N01: 0 protected handler calls.
- Five independent held-out reproductions: all lacked graph-mutation authority; 0 protected handler calls, confirmation stages, or state changes.
- Fresh held-out no-consent corpus: 605 cases; 0 wrong positive authorizations, protected handler calls, confirmation stages, or state changes; 9/9 true-positive controls accepted.
- Operation-binding matrix: 26 cases; all 17 invalid plans blocked before the protected handler; 9/9 exact matching plans executed through preview/confirmation; 0 wrong valid-plan blocks.
- Invalid substitutions blocked: 6 operation-family, 6 target, 2 parameter, 2 mixed authorized/denied, and 1 extra-operation case.
- S10-N02 proofs A through D: all blocked before the protected handler.
- Stage 10 Corrective #1 corpus preserved: 640 no-consent plus 11 positive cases, with every violation counter zero.
- Stage 8 gates preserved at their approved counts, including S8-N03 through S8-N06.

## Source-level anti-pattern review

Manual source inspection found no exact-five-phrase-only fix, global negation flag, mutation-verb-as-consent rule, or generic side-effect-class final authorization. Broad `graph_edit_preview` checks are preliminary only. The final dispatcher resolves canonical `domain`/`typedKind` identity, reconstructs deterministic operation semantics from the original query, and requires exact operation authorization before protected-handler reach. UI confirmation staging repeats the operation-bound gate. Model/prepared-plan metadata is not authority; missing state-changing request semantics fail closed; legacy public-registry side effects remain authoritative; target identifiers and material parameters are compared.

## Regression and dependency qualification

- Node suite: 219/219 files passed in the source tree and the new fresh extraction.
- Lint: passed.
- Normal and GitHub Pages builds: passed; Vite reported only the existing bundle chunk-size warning.
- Full audit: one high-severity, transitive development-only Browserslist advisory (`GHSA-c83g-rgw3-j3cx` and `GHSA-73wf-gq98-2v4g`); a fix is available, but dependency changes were not authorized.
- Production audit: 0 vulnerabilities.
- `git diff --check`: passed before evidence commits.
- No package or lockfile changes; package version remains 7.3.14.

## Portable package

`hypergraph-converter-v7.3.14-stage10-corrective2-portable.zip` is 1,211,026 bytes with SHA-256 `f960f581eeb246157b872a5b32c144eacb717a695540a5361d99b44b90645bb5`.

The archive has 660 members: 632 files and 28 directories under the single `hypergraph-converter-v7.3.14` root. Every member was compared with the fresh extraction before install: 0 missing, 0 byte mismatches, and 0 unexpected entries. CRC/testzip passes. R08 remains closed: all 660 entries use Unix `create_system`; all 28 directories and 17 intended launchers are 0755; all 615 ordinary files are 0644. All six shell launchers have a Bash env shebang and LF-only endings. No absolute, traversal, backslash, duplicate, case-collision, symlink, special, forbidden, unexpected-executable, group/world-writable, or non-Unix metadata entries were found. Two Windows and two WSL/Ubuntu packings produced the identical SHA-256.

Five of six shell launchers were directly invoked under bounded WSL execution and either passed or produced the expected environment-dependent safe exit. `run-with-ollama.sh` was not directly executed because the requested elevated run was not approved: the script can start Ollama, pull model weights, install packages, start a bridge, and expose a development server. Its archive mode, shebang, LF endings, and source syntax remain qualified.

## Clean extraction and browser

The new extraction contained no `.git`, `node_modules`, `dist`, or `artifacts` before install. A clean `npm ci` added 144 packages. The expanded corrective tests ran as part of the clean 219/219 Node suite; lint, both builds, and both audits reproduced the source-tree results.

Real Chrome loaded a two-hyperedge graph at version 1, displayed Graph Preview, ran Connected Components, and generated a CSR CSV preview. The exact original plus all three required held-out no-consent utterances each staged 0 confirmations and left version 1 unchanged. A valid create-hyperedge request staged exactly one preview/confirmation without silent commit. Cancel preserved version 1 and two hyperedges. Restaging and explicitly confirming advanced the graph to version 2 with three hyperedges, five vertices, six incidences, and `h3` committed. Application-origin console errors: 0. Application-origin console warnings: 0. Sixteen warnings from Chrome extensions were excluded by origin.

## Remaining limitations

The development dependency audit remains non-zero because of the transitive Browserslist advisory, and the production JavaScript bundle still triggers Vite's chunk-size advisory. One side-effectful launcher was not directly executed because approval was not granted; its static archive requirements passed. Dependency changes and broader performance refactoring were outside this corrective's authorization.
