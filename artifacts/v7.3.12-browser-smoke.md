# v7.3.12 browser smoke

Status: not run in this environment.

The automated validation completed successfully (`npm run lint`, `npm run build`, `npm run build:github`, and `npm run test`), but I did not perform an interactive browser session or console inspection. This artifact exists so the release does not silently over-claim browser validation.

Recommended manual smoke:

1. Start the app with `npm run dev`.
2. Open the dashboard in a browser.
3. Load a small graph and verify the deterministic assistant still handles read-only questions without staging mutations.
4. Exercise Tools tab keyboard navigation with ArrowLeft, ArrowRight, Home, End, Enter, and Space.
5. Open visualizations/mappings/exports and confirm no console errors.
