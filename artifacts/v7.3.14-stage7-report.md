# Hypergraph Converter Studio v7.3.14 — Stage 7 Report

## Status

PASS for the authorized Stage 7 performance, lazy-derived-state, worker, cancellation, and compact-trace scope. The only open gate observation is one newly reported high-severity advisory in the pre-existing dev-only `browserslist` tree; production dependency audit is clean, and dependencies were protected from Stage 7 changes.

## Baseline and commits

- Approved parent: `f93bb322a7cfca06123e12148850fc5278ef3726`
- Pre-change characterization: `162c8c5` (`stage-7: record prechange performance contracts`)
- Production implementation: `add08af` (`stage-7: add lazy derived workers and compact traces`)
- Diff-review correction: `4caef42` (`stage-7: keep line graph cache projection-scoped`)
- Validation evidence: recorded in a separate final Stage 7 commit

The parent was clean and exact before work. Package/package-lock versions remain 7.3.13. No approved Stage 0–6 commit was amended or rewritten.

## Issues addressed

All five registered targets are RESOLVED: HG713-C03, HG713-C04, HG713-C05, S4-OBS01, and the Stage 6 shortest trace-amplification observation. Exact dispositions and evidence links are in `v7.3.14-stage7-issue-register.json`.

## Pre-change evidence

The independent pre-change commit captured the exact Stage 6 parent. On ordinary graph load it reproduced unconditional H2V, V2H, CSR, statistics, H2V/V2H text, and full row creation. LargeSparse retained 10,000 H2V rows and 10,001 V2H rows before either was selected. H2H/V2V were already request-gated and stayed so.

The prior Matrix path used `h.vertices.map(String).includes(v)` per V×H cell. Pre-change dense cases measured 45.39 ms (200²), 89.93 ms (300²), and 205.95 ms (400²) on Node v24.19.0 / Intel Core Ultra 5 225F. The pre-change 5K BFS/DFS eager traces retained more than 20 million duplicated visited/frontier references; far-target 1K shortest retained more than 400,000 visited references. Timings and heap deltas are machine evidence, not universal promises.

## Lazy derived products and bounded presentation

The production cache key includes graph version, operation, and stable options. Graph identity/version replacement clears entries. Failed, cancelled, stale, and over-budget states are refused as successful cache writes.

Production-seam counters show:

- default H2V mapping: H2V 1; V2H/CSR/Matrix/export text 0;
- selecting V2H: V2H becomes 1; CSR/Matrix remain 0;
- requesting CSR CSV: CSR becomes 1; Matrix remains 0;
- repeated exact CSR export: one construction plus one cache hit;
- graph replacement: four complete old-version entries become zero;
- two incomplete/refused writes were rejected.

Mapping display materialization preserves 10,000 exact underlying items while creating 500 display rows, reports totalRows=10,000 and complete=false, and preserves ID 0, literal `null`, `__proto__`, Unicode, and weight 0. Exact export is a distinct request-scoped path. Exact graph statistics remain eager because both top cards and deterministic agent state continuously consume them; the pre-change evidence measured that linear builder rather than assuming it away.

## Matrix correctness and performance

The Matrix path now preprocesses one `Set` per hyperedge and performs `members.has(vertex)` per output cell. The prior nested array reconstruction/scan is structurally absent. Existing 1,000,000-cell and 4,000,000-estimated-byte budgets are unchanged.

The focused gate passed 5,000 seeded random graphs plus zero/prototype/null/Unicode/empty/singleton/duplicate/CRLF/CSV fixtures with exact byte equality. The final full-suite run measured 4.84 ms / 11.76 ms / 26.99 ms for 200² / 300² / 400². The generated oracle independently compared indexed and legacy text and reported exact matches for all three dimensions.

## Workers, cancellation, and races

Measured thresholds select a Worker above 50,000 H2H neighbor references, V2V candidate pairs, or Matrix cells. Tiny operations remain deferred-local. The Vite build emits the dedicated derived Worker asset.

The request coordinator carries `graphVersion`, `requestId`, `operationType`, and options. Nine deterministic scenarios passed: graph replacement, graph edit, switching away, rapid view switching, stats graph change, export cancellation, A completing after B, worker failure, and unmount. Each asserts current graph authority, separate cancellation/stale rejection, settled loading, no stale rows/text/edges/download, no duplicate completion, and no unhandled rejection.

## Trace semantics and retained state

The compatibility facade remains an Array with the exact original step count and numeric access behavior. Only one requested full step is cached. The UI materializes the current animation step with a bounded cache.

Across 2,000 random graphs, 86,855 fully materialized BFS/DFS/Shortest steps matched the approved eager implementations with zero mismatches. OneHugeEdge5K BFS and DFS each retained 5,000 scalar records, zero full snapshots, and zero cached full steps before access; BFS stored 4,999 discovery deltas and DFS stored no duplicated full-step arrays. Far-target OneHugeEdge1K shortest and LargeSparse shortest likewise retained zero full snapshots/cached full steps.

On the evidence machine, 5K BFS ran in about 462 ms and 5K DFS in about 1,673 ms; this Stage resolves retained-trace amplification, not the mathematical cost of dense incidence traversal. No FPS or universal browser-performance claim is made.

## Real Chromium validation

The application was exercised in the real Codex Chromium controller at `http://127.0.0.1:5173/`. The 16 required checks passed: ordinary load, selected H2V/V2H/H2H, CSR and Matrix exact exports, Line Graph computing state, graph replacement during work, rapid view switching, truthful resource refusal, pan, zoom, node drag, ring selection, weight 0, searches for 0/`__proto__`/literal null, resize, Force Reheat, algorithm step forward/back, and zero console errors. No FPS or screenshot claim is made. Detailed observations are in `v7.3.14-stage7-browser-validation.json`.

## Regression gates

- Stage 7 focused tests: PASS (5/5 files)
- Full Node suite: PASS (201/201 files, 59.4 s)
- Stage 0 preservation and all Stage 1–6 original/corrective gates: PASS inside the full runner
- Lint: PASS
- Production Vite build: PASS; 205 modules and dedicated 11.88 kB Worker asset
- GitHub Pages build (`/hypergraph-converter/` base): PASS
- `npm audit`: 1 high-severity dev-only `browserslist` advisory
- `npm audit --omit=dev`: PASS, 0 vulnerabilities
- `git diff --check`: PASS
- Existing ZIP-permission test: environment SKIP because `python3` is unavailable; unchanged and unrelated to Stage 7

The >500 kB main bundle warning remains advisory and pre-existing in character. No dependency, package version, assistant/NLU, parser, packaging, or Stage 5 Force constant was changed.

## Risks and recommended next action

Compact DFS step access replays deterministic traversal to reconstruct a requested step, trading retained memory for CPU; the animation UI requests only one step at a time. Worker availability still depends on browser Worker support, with a structured deferred-local fallback. The dev-only `browserslist` advisory should be handled in a separately authorized dependency-update stage so its lockfile effects receive independent review.

Recommended next action: independent Stage 7 review against the artifacts, complete diff, real-browser record, and protected prior-stage gates. Do not begin Stage 8 until that review approves Stage 7.
