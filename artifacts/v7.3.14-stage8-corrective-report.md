# Stage 8 corrective validation

- Baseline commit: `cec0e0ce33bc1a3b6b77916969787ebcdcbc0d00`
- Approved Stage 7 ancestor: `32e4fca5ac2ca1229cceef260b2fa8827415887f`
- Production corrective commit: `920344d19d41ca37e386f2a89048cc741eba097d`
- Package version: `7.3.13`
- Stage 9 begun: false.

## Corrective results

- S8-N01 closed: the strong mixed-clause oracle covered 1,135 cases with zero unauthorized side effects, zero missed authorized clauses, zero wrong side-effect scopes, and zero state-fingerprint violations.
- S8-N02 closed: all eight >5,000-character adversarial probes failed closed at the central authorization gate; no protected handler ran and no protected state changed.
- Model/fallback authority: eight malicious or mismatched model/fallback plans executed zero side effects, caused zero protected-state violations, and remained subordinate to the deterministic authorization contract.
- Existing read-only corpus: 9,840/9,840 safe.
- Independent read-only corpus: 6,560/6,560 safe.
- Positive corpus: 492 cases with zero wrong blocks.
- Pending corpus: 30/30 read-only requests preserved pending state; 3/3 genuine corrections routed correctly.

## Command validation

- Full Node suite: passed, 210/210 files in 80.2 seconds. The existing ZIP-permission check was skipped because `python3` was unavailable in this environment.
- Lint: passed.
- Production build: passed with the existing Vite >500 kB chunk warning only.
- GitHub build: passed with the same existing chunk warning only.
- Full `npm audit`: one high-severity dev-only `browserslist <=4.28.6` advisory. No dependency was changed because this corrective explicitly prohibits dependency changes.
- Production dependency audit (`npm audit --omit=dev`): zero vulnerabilities.
- `git diff --check`: passed.

## Real Chromium closure

All 16 required flows passed in real Google Chrome through the computer-use extension at `http://127.0.0.1:5173`. Direct graph staging, mixed-clause routing, pending correction, confirmation, cancellation, mapping/grouping/parser speech-act separation, central blocking of a schema-valid local-model mutation plan, and CSR navigation were observed in the rendered app. Both inspected Chrome tabs reported zero console errors.

The model-authority flow used a temporary loopback-only Ollama-compatible stub returning a schema-valid `CLEAR_GRAPH` ActionPlan. The dashboard accepted the plan as untrusted data, then the deterministic central gate blocked it because the user explicitly requested no changes. The graph remained unchanged. The temporary stub was stopped and removed after the test.

Chrome extension file-URL access was unavailable, so file-upload activation was not forced by changing browser settings. The mapping, grouping, and parser imperative/question pairs were instead verified through the rendered app's no-active-batch context gates.

## Scope closure

Only Stage 8 corrective production code, tests, harnesses, and evidence were changed. Package and dependency manifests remain at the approved baseline state. Stage 9 was not begun. The corrective is ready for independent Stage 8 review.
