# Stage 8 corrective #3 validation

- Authoritative parent: `6b0af8a8a33e5dccc0a9cb7f336d82e1bddaa1e7`
- Pre-fix evidence commit: `9737002a3dd67cf08b825ec489fc134b90d89851`
- Production corrective commit: `54af3489a1a0253502629585dffcd19635cb5c4b`
- Package version: `7.3.13`
- Stage 9 begun: false.

## Findings and correction

- S8-N05 (High) was independently reproduced before the production change. `resolveActualPlanIdentity()` now validates one registered domain/typed-kind identity, derives its actual side-effect class and operation types, and supplies the only domain used by handler routing. A mismatch fails at `deterministic_final_plan_identity_gate` with `plan_identity_mismatch`; no protected handler runs.
- Nominally operation-free `DashboardControlIntent`, `LegacyActionIntent`, `GroundedQuestion`, and `DeterministicHelpQuery` payloads reject non-empty operation arrays with `unexpected_state_changing_payload` rather than ignoring contradictory mutation material.
- S8-N06 (High) was independently reproduced before the production change. `LegacyActionIntent` now resolves through the existing public action-intent registry. Its registry entry is authoritative for side effect and identity. Supplied metadata is comparison-only; a mismatch fails closed with `legacy_side_effect_metadata_mismatch`, and an unregistered action fails closed.
- Safety-converted questions and incomplete legacy commands now expose a canonical `grounded_question` / `GroundedQuestion` outer identity while retaining the original compilation as non-executable audit context.

## Adversarial gates

The identity-corruption matrix contains 41 cases. Every protected family is paired with every wrong typed kind at least once, including 12 read-only typed-kind pairings. All identity mismatches used the precise fail-closed reason, protected handler calls were zero, and protected-state violations were zero. Four nominally read-only payloads containing operation material were also blocked with zero handler calls.

The legacy-authority matrix covers all 49 public registered actions under correct, omitted, `read_only`, unrelated-valid, and unknown supplied side-effect metadata, plus kind-only and nested `compiled.action` privilege corruptions and an unregistered kind: 248 cases total. The 101 expected positive executions all ran, while metadata-bypass calls, corrupt-metadata handler calls, unregistered handler calls, and protected-state violations were all zero.

The model-shaped plan with `domain = graph_mutation`, `typedKind = DashboardControlIntent`, and `CLEAR_GRAPH` payload material was blocked with `plan_identity_mismatch`; neither graph nor dashboard handler ran, no confirmation was staged, and state was unchanged. The known state-changing legacy action mislabeled `read_only` was blocked by the registry comparison.

## Approved Stage 8 preservation

- Original corrective #2 matrix: 125 cases, exactly five expected and actual executions, zero violations.
- S8-N01 mixed corpus: 1,135 cases; zero unauthorized side effects, missed authorized clauses, wrong side-effect scopes, and state-fingerprint violations.
- S8-N02 truncation probes: 8/8 blocked; zero handler calls and protected-state violations.
- S8-N03 and S8-N04: the existing declared/actual class and no-semantics final-gate behavior remains unchanged.
- Existing read-only corpus: 9,840/9,840 safe.
- Independent read-only corpus: 6,560/6,560 safe.
- Positive corpus: 492 cases with zero wrong blocks.
- Pending corpus: 30/30 read-only requests preserved the pending action; 3/3 genuine corrections succeeded.
- Model/fallback corpus: eight cases with zero malicious execution.

## Real Chrome validation

Six focused flows passed in real Google Chrome at `http://127.0.0.1:4175/`: normal graph staging; CSR navigation; a public legacy Export navigation; inert read-only graph explanation; malicious model-plan blocking; and pending cancel, correction, and confirm lifecycle. The confirmed correction produced a verified graph with three hyperedges, six vertices, and ten incidences.

The malicious model flow used a temporary loopback-only Ollama-compatible fixture returning a schema-valid `CLEAR_GRAPH` ActionPlan to the read-only query `Run a graph operation without changing anything.` The UI reported that the plan lacked positive state-changing authorization, created no pending confirmation, and preserved the graph. The fixture stored no data, contacted no external service, and was stopped and removed after validation. Chrome reported zero console errors.

## Command validation

- Full Node suite: passed, 217/217 files in 94.6 seconds. The existing v7.3.11 ZIP-permission test was skipped because `python3` was unavailable in this environment.
- Lint: passed.
- Production build: passed with the existing Vite >500 kB chunk-size warning only.
- GitHub build: passed with the same existing warning only.
- Full `npm audit`: one high-severity dev-only `browserslist <=4.28.6` advisory. No dependency was changed because this corrective explicitly prohibits dependency changes.
- Production dependency audit (`npm audit --omit=dev`): zero vulnerabilities.
- `git diff --check`: passed.

## Scope closure

The package and lockfile remain at version `7.3.13`, dependency manifests are byte-for-byte unchanged from the authoritative parent, and S8-N01 through S8-N04 were preserved rather than redesigned. Only the Stage 8 corrective #3 identity/authority boundary, narrowly required canonical question metadata, permanent adversarial tests, and evidence were changed. Stage 9 was not begun.
