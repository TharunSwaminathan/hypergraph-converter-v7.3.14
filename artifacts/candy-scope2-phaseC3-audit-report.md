# CANDY Scope 2 Phase C3 independent audit

Result: **PASS** — all Critical/High findings closed. Open Critical: 0. Open High: 0.

The C3 audit exercised deterministic precedence, local/runtime capability intersection, graph-type gating, confirmation bindings, bounded observations, unsupported remote tools, stale graph state, absent/unauthorized/incompatible companion behavior, page refresh, and prompt-injection strings in native-looking results.

The feature flag defaults off. Mode A remains the existing Studio. Mode B stores its pairing credential only in `sessionStorage`, never in React state passed to persistence/model observations. Runtime declarations cannot create tools: only the locally compiled `RUN_SSSP` identity/version/backend/modes/type intersection survives. ReAct exposes five product actions and no process primitive. Submission stops the loop; later status/result requests re-observe the companion. COMPARE or requests above fixed thread/timeout/cardinality thresholds require confirmation bound to request arguments and graph identity/version.

The real-browser C3 audit found a Critical hook-order defect after the first integration: `useCandyRuntime` had been placed behind an existing conditional return, so changing graph availability changed AppCore's hook sequence. Execution was stopped, the hook was moved into AppCore's unconditional hook sequence, and feature-disabled behavior remains internal to the hook. A focused source-order regression plus a cold browser none-to-loaded graph transition and reload now pass with zero React console warnings/errors.

Live qwen3:8b initially produced a factually unsafe read-only explanation that implied hypergraph traversal. Execution safety remained fail-closed—the model performed no action—but the semantic explanation was incorrect. C3-H01 was fixed by moving active-graph CANDY explanation and permanent Hypergraph rejection to a deterministic pre-model boundary. The guard derives accepted types from the qualified Scope 1 graph-type contract. Retests show the exact read-only request creates no job or confirmation; the executable request returns `INVALID_GRAPH_TYPE`, explicitly records no projection, and creates zero artifacts/jobs/processes. Raw stdout/stderr, arrays, tokens, and prompt-like strings are absent from model observations.

The focused final qwen run then exposed C3-H03 (High): qwen selected the correct typed submit action but omitted required bounded resource fields. Strict validation rejected it and nothing executed. The prompt now exports a compiled, non-authoritative argument contract for `SUBMIT_CANDY_JOB` (including bounded defaults), while the validator remains authority. The repeated live run accepted submit/status/cancel with zero schema failures and zero fallback. This did not create a shell, path, process, environment, or arbitrary runtime field.

Final diff review found C3-H02: the first CANDY authorization adapter accepted a shared verb regex for both submission and cancellation, so a wrong model action family could borrow positive authorization from another CANDY verb. The existing read-only, graph-type, strict-schema, and routing boundaries constrained reach, but the final action boundary was not exact enough. It now requires a positively authorized clause, CANDY/job topic context, and an action-specific verb family: run/submit/start/execute/compute/calculate/find for submission; cancel/stop for cancellation. Negated, unrelated, and cross-family tests fail closed.
