# Stage 9 — packaging, versioning, and clean-extraction qualification

- Authoritative parent: `1aec97577efbf734254ca340b6b5a07455fb3271`
- Versioning commit: `cd28437d7b9e6cbf09bfd2ecdd4e8b8486713d98`
- Packaging/fix commit: `6cfb2fe87313ad0597ef3eb08c7ed48f90fee829`
- Package version: `7.3.14`
- Stage 10 begun: false

## Version and dependency policy

The intended package version is now 7.3.14 in `package.json` and both lockfile root version fields. README and MODEL_SETUP identify v7.3.14 as current without rewriting historical release references, and `docs/RELEASE_v7.3.14.md` records the release scope.

No dependency or resolved lockfile dependency changed. The full audit still reports one high-severity development-only browserslist advisory through `eslint-plugin-react-hooks -> @babel/core -> @babel/helper-compilation-targets -> browserslist@4.28.2`. The apparent update also refreshes browser-targeting data packages and could affect output, so Stage 9 leaves it for separate authorization. `npm audit --omit=dev` reports zero vulnerabilities.

## Authoritative package and R08

`scripts/package-portable-source.py` is the sole release packager. It produced `hypergraph-converter-v7.3.14-portable.zip` outside the repository working tree. The 1,194,025-byte archive has SHA-256 `475f9eaa0549500bbc07b6aa823507899f810be0ecef70f5de834f45001da3b2`, 655 members, and exactly one root named `hypergraph-converter-v7.3.14`. A second packaging run produced the same SHA-256.

The actual ZIP passed CRC/testzip and every package-specific adversarial check: no traversal, absolute or backslash paths, duplicate members, case collisions, forbidden content, symlinks, special files, unexpected executables, missing executable bits, writable ordinary files, or root-layout defects. All 655 entries use Unix `create_system == 3`. Modes are 610 entries at 0644 and 45 at 0755 (28 directories plus 17 intended `.sh`, `.bat`, and `.ps1` launchers).

R08 is closed pending independent review. The exact final ZIP was extracted in WSL2; all six shell launchers had mode 0755, `#!/usr/bin/env bash`, LF-only text, and were invoked directly as `./<launcher>.sh`. The diagnostic and safe configuration examples completed. Runtime-starting scripts exited with actionable expected errors because that WSL distribution has neither Node.js nor Ollama. No bridge process was left running.

## Fresh extraction and full qualification

The archive was extracted outside the working tree at `C:\Users\tharu\OneDrive\Documents\Candi\stage9-clean-extraction-475f9eaa\hypergraph-converter-v7.3.14`. Before install, all 627 archive file members existed with exact uncompressed sizes, all 28 directory members were accounted for, and `.git` was absent.

`npm ci` installed 175 packages from the lockfile. From that clean extraction, the full Node suite passed 217/217 files in 87.4 seconds with the ZIP-permission test active, lint passed, the standard Vite build passed, and the GitHub-base build passed. Both builds retained only the known >500 kB chunk-size advisory. The full audit reported the one dev-only browserslist advisory; the production audit remained clean.

All Stage 0–7 named preservation suites passed. Stage 8 retained the required 1,135 mixed cases with all four violation counters at zero; 8/8 truncation blocks with zero handlers; 9,840/9,840 and 6,560/6,560 read-only safety cases; 492 positives with zero wrong blocks; 30/30 pending preservations and 3/3 corrections; eight model/fallback cases with zero malicious executions; 125 corrective #2 cases with exactly five executions and zero violations; 41 corrective #3 identity cases with zero protected handlers/state violations; and the 248-case, 49-action legacy matrix with 101 expected/actual executions and every bypass/handler/state violation counter at zero.

## Real Chrome release smoke

The clean standard release build was exercised in real Google Chrome. A real H2V graph converted to 3 hyperedges, 5 vertices, and 9 incidences. Hypergraph Preview, Force layout, Reheat simulation activity, vertex/ring selection, search, Line Graph round-trip, BFS, Statistics, and CSR export all behaved correctly. A replacement graph using `__proto__`, `constructor`, `toString`, and `hasOwnProperty` advanced the graph version and was reflected in counts, mappings, Preview, Algorithms, Statistics, and export output.

The read-only assistant query `Explain graph clearing without changing anything.` caused no mutation and staged no confirmation. The positive control `Create hyperedge h3 with vertices 8 and 9.` produced one preview/confirmation card and did not commit silently. Application-origin console errors and warnings were zero; eight observed warnings belonged only to installed Chrome extensions.

The managed Chrome automation surface exposed a fixed 1718x1270 viewport. Its fullscreen, zoom, and device attempts did not change the viewport, while the page DOM is deliberately read-only, so the smoke could not capture a second real canvas dimension. The canvas rendered at 1354x520, all other Preview interactions passed, and the clean-extraction ResizeObserver/canvas-size regressions passed. This environment qualification is recorded rather than misrepresented as a real resize.

## Scope closure

Stage 9 changed only intended version reporting, release documentation, portable packaging/verification, package-self-contained test evidence, and Stage 9 evidence. Stages 0–8 remain preserved. No Stage 10 audit was begun; this release candidate is stopped for independent Stage 9 review.
