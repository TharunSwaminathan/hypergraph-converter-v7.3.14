# v7.3.13 browser smoke

Browser: Codex In-app Browser.

Build under test: production root-base build served with Vite preview.

URL:

```text
http://127.0.0.1:4173/
```

Result: PASS.

Verified:

- Dashboard shell rendered with `Hypergraph Converter Studio`.
- Route controls rendered.
- Hypergraph Assistant rendered.
- Algorithms panel entry rendered.
- Assistant Settings opened.
- Local/Ollama assistant settings rendered.
- Local Runtime Diagnostics opened and displayed direct Ollama/local bridge status rows.
- Browser console error count: `0`.

Note: A GitHub-base build was also produced successfully with `npm run build:github`. Local browser smoke used the root-base production build because Vite preview served the GitHub-base asset path as fallback HTML in this local environment; the GitHub build itself passed separately.
