# v7.3.14 Post-Qualification Corrective A — final evidence

Corrective A is closed pending independent review. This report does not approve or begin Corrective B, C, or D, and no release ZIP was generated.

## Commit chain

- Parent: `b9159e2bae3d1548f2b0a47d86cbe4e75cdc55e0`
- Pre-change characterization: `a182d509eccdaab1b060600ce5481808191dd18e`
- Metadata-bearing H2V corrective: `4cc87282cba7fa23fea7b85e312cca842ea85468`
- Singular `.edge` corrective: `5b5661e12b54cc21238ebc3435ca162cbdb2d6bb`
- Naming-evidence hardening: `6279cf41b5a50f9c0cec1aba36fac78ad73d64f7`

The final evidence commit is recorded after this report is committed. The two issues remain `closed_pending_independent_review`; neither is represented as independently verified.

## Corrective scope

The H2V detector now recognizes the parser's supported metadata-bearing left-hand side instead of sending the built-in H2V example to adjacency parsing. Strong-evidence recognition was narrowed so arbitrary identifier shape alone does not force H2V; genuinely ambiguous single-line colon forms fail closed and keep both manual routes available.

The upload policy and file-detection paths now share case-insensitive singular `.edge` support while preserving `.edges`. The policy still rejects the near-miss `.edg`, retains the existing file-count and byte limits, and does not bypass the normal Edge List parser.

The production diff from the authoritative parent through naming hardening changes `src/App.jsx`, `src/agent/datasetProfiler.js`, `src/agent/fileDetection.js`, `src/agent/uploadPolicy.js`, `src/components/AgentChatPanel.jsx`, `src/hooks/useFileUpload.js`, and `src/utils/parsers.js`, plus focused tests and pre-change evidence. It contains 237 insertions and 20 deletions across ten files before the final evidence-only commit.

## Differential and oracle evidence

The deterministic corpus contains 131 cases: 115 strong-evidence inputs and 16 intentionally ambiguous inputs. It covers metadata-bearing and named H2V, adjacency, quote-aware and ordinary CSV, H2H, whitespace-delimited rows, reserved object-key identifiers, numeric zero, multiple whitespace forms, and ambiguity controls.

All 115 strong-evidence cases selected the intended route and produced the same normalized graph as the intended parser. Wrong routes were 0 and silent semantic changes were 0. All 16 ambiguous cases returned no automatic route with `simple` and `adjlist` candidates. Extension controls accepted `.edge`, `.EDGE`, `.EdGe`, and `.edges`, routed each to `edgelist`, and rejected `.edg`.

## Controlled real-browser smoke

A standard production build was served with:

`node .\node_modules\vite\bin\vite.js preview --host 127.0.0.1 --port 4173 --strictPort`

Chrome was explicitly navigated in a newly opened tab to `http://127.0.0.1:4173/`. Before interaction, the address and page identity established scheme `http`, host `127.0.0.1`, port `4173`, and `Hypergraph Converter Studio` content.

- The built-in metadata-bearing H2V example auto-detected as `simple` / H2V, converted to 3 hyperedges, 5 vertices, and 9 incidences, and produced matching mappings and Preview content with no metadata-fragment identifiers.
- The built-in adjacency example auto-detected as `adjlist` and converted to 4 hyperedges, 4 vertices, and 8 incidences.
- The quote-aware CSV fixture `"paper:A","Alice, Bob"` plus `"paper:B",Carol` auto-detected as `csv`, converted to 2 hyperedges, 4 vertices, and 4 incidences, and retained the quoted comma as one field (row cardinality 2).
- `corrective-a.edge` and case-variant `corrective-a.EDGE` were each selected through Chrome's native file picker, accepted, and auto-detected as `edgelist`. The lowercase fixture converted to 2 hyperedges, 3 vertices, and 4 incidences.
- `.edges` preservation is covered by the shared automated regression and was not redundantly uploaded in this browser run.

Chrome DevTools showed zero application-origin uncaught errors and zero application-origin warnings. Four warnings and one `runtime.lastError` were attributable to installed assignment/Grammarly/browser-extension scripts; they are recorded separately in the browser artifact and excluded from the application result.

The earlier pre-resume browser attempt stopped because the computer-use environment could not establish the current Chrome URL safely. The resumed run did not reuse that unknown page: it opened a new tab, entered the controlled loopback URL, verified it, and completed the smoke. This history is an environment qualification, not a product failure.

## Regression, build, and dependency evidence

- Node suite: 221/221 files passed in 109.5 seconds.
- Lint: passed.
- Standard production build: passed; 207 modules transformed, with only the existing Vite large-chunk advisory.
- GitHub-base build: passed; 207 modules transformed, with only the existing Vite large-chunk advisory.
- Production audit: `npm audit --omit=dev` reported zero vulnerabilities.
- Full audit: one high-severity development-only Browserslist advisory remained unchanged.
- Dependency changes: none.
- Package version changes: none; version remains 7.3.14.
- Release ZIP generated: no.

## Review boundary

The evidence demonstrates the intended corrective behavior in deterministic tests and in a controlled real browser. `POST-S10-H2V-01` and `POST-S10-EDGE-01` remain `closed_pending_independent_review`. Independent reviewers should use the no-Git handoff, reproduce the commands, and decide whether to accept closure.

STOPPED — awaiting independent Corrective A review.

DO NOT BEGIN CORRECTIVE B.

DO NOT BEGIN CORRECTIVE C.

DO NOT BEGIN CORRECTIVE D.
